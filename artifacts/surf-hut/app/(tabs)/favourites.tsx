import React from "react";

import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function FavouritesTab() {
  return (
    <PlaceholderScreen
      icon="heart"
      title="Your home breaks"
      body="Tap the heart on any beach to save it here. Open the app, glance at your locals — that's it."
      accent="primary"
      footer="STORED ON DEVICE  ·  NO ACCOUNT NEEDED"
    />
  );
}
